{
  "name": "@launchframe/mcp",
  "version": "1.1.14",
  "description": "LaunchFrame MCP Server — knowledge tools for AI agents building LaunchFrame projects",
  "bin": {
    "launchframe-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc && cp -r src/content dist/content && chmod +x dist/index.js",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build && chmod +x dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^22.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "type": "module"
}
